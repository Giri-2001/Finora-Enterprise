import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.finora.enterprise',
  appName: 'FINORA Enterprise',
  webDir: 'dist'
};

export default config;
